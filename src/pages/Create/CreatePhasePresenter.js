import React from "react";
import CreatePhase from "../../components/Create/CreatePhase";

export default function CreatePhasePresenter({ phaseData }) {
  return (
    <>
      <CreatePhase data={phaseData} />
    </>
  );
}
